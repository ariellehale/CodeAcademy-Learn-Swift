// Convert the temperature to Celsius using the formula provided. Store the result in tempc.

var tempf: Double = 80
var tempc: Double = (tempf - 32) / 1.8

print("The temp is \(tempc) degrees Celsius.")